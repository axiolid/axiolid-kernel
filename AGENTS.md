# Axiolid contribution rules

Read `HERMES.md` and the closest nested `AGENTS.md`. Preserve the pure-Rust, format-agnostic boundary: no IFC, file-format, or vendor model types in `crates/`. Run the focused crate checks while iterating and `scripts/gate.sh` before landing workspace-wide changes.
