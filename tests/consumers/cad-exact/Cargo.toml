[package]
name = "axiolid-consumer-cad-exact"
version = "0.0.0"
edition = "2021"
publish = false

# Empty table: this fixture is its OWN workspace root, so workspace feature
# unification cannot mask what a real downstream application resolves.
[workspace]

[dependencies]
axiolid-brep = { path = "../../../crates/representations/brep", default-features = false }
axiolid-curve = { path = "../../../crates/representations/analytic/curve", default-features = false }
axiolid-nurbs = { path = "../../../crates/algorithms/parametric/nurbs", default-features = false }
axiolid-surface = { path = "../../../crates/representations/analytic/surface", default-features = false }
axiolid-topology = { path = "../../../crates/representations/topology", default-features = false }
