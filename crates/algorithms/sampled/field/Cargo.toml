[package]
name = "axiolid-field"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
authors.workspace = true
description = "Frame-neutral layered spatial fields: triangle coverage, morphology, clearance."

[features]
default = []
# Geometry-only traversal primitives. Opt-in until a second consumer needs the
# same neutral contract; see navigate.rs for the promotion rule.
navigation = []

[dependencies]
axiolid-core.workspace = true

[package.metadata.axiolid]
architecture-version = 1
layer = "algorithms"
role = "algorithm.sampled.mixed"
domain = "sampled.temporary"
public = true
format-neutral = true
allowed-internal-dependencies = ["axiolid-core"]
